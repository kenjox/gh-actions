name: 05 - 01 - Filters Activity Types

on:
  workflow_dispatch:
  # pull_request:
  #   types: [opened, synchronize]
  #   branches:
  #     - 'main'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: A new PR to main branch
        run: echo "Running whenever a PR is opened or synchronized AND base branch is main"
