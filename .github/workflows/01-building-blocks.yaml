name: 01-Building blocks

on: [workflow_dispatch]

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo 'Hello, World!'
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Successfully finished
        run: |
          echo 'I have successfully finished running'
          exit 0
      - name: Say goodbye
        run: echo 'Goodbye!'
